* robots.txt and/or site-map: prevent Google from indexing URLs with date (e.g. 2010/04/10)

* save date of DB update & return Last-Modified header
* support If-Modified-Since
* don't update datastore entries if the menu webpage was not changed

Later:
* add /about.html
  - requires refactoring of JSP pages & CSS styles

Next versions:
* Forum via twitter? (Find tweets containing a special short url, that points to our website).
